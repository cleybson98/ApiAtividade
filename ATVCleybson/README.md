Instruções rápidas:
1. Build: `mvn clean package`
2. Run: `mvn spring-boot:run` (ou executar o jar em target/)
3. Endpoints:
   - POST /api/livros
   - GET /api/livros
   - GET /api/livros/{id}
   - PUT /api/livros/{id}
   - DELETE /api/livros/{id}

Banco: H2 (arquivo em ./data/librarydb)
